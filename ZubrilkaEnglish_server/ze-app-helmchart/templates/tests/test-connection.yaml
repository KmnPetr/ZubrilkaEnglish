apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ze-app-helmchart.fullname" . }}-test-connection"
  labels:
    {{- include "ze-app-helmchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ze-app-helmchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
