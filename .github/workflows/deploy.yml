on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

#      - name: Change directory
#        run: cd ZubrilkaEnglish_server

#      - name: Set up Java
#        uses: actions/setup-java@v2
#        with:
#          distribution: adopt
#          java-version: 17

#      - name: Mvn Clean Package
#        run: mvn -f ZubrilkaEnglish_server clean package
#

#      - name: Login to Docker Hub
#        uses: docker/login-action@v1
#        with:
#          username: ${{ secrets.DOCKERHUB_USERNAME }}
#          password: ${{ secrets.DOCKERHUB_TOKEN }}

      #      - name: Docker Build
      #        run: docker build -t kmnpetr/ze ./ZubrilkaEnglish_server

#      - name: Stop APP on server
#        uses: appleboy/ssh-action@master
#        with:
#          host: ${{ secrets.SERVER_IP }}
#          username: ${{ secrets.SERVER_USERNAME }}
#          password: ${{ secrets.SERVER_PASSWORD }}
#          script: |
#            docker ps -a || true
#            docker compose -f /home/ze/docker-compose.yml down || true
#            docker images || true
#            docker rmi kmnpetr/ze || true



      - name: Send docker-compose.yml to server
        uses: actions/checkout@v2
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USERNAME }}
          password: ${{ secrets.SERVER_PASSWORD }}
          source: "./ZubrilkaEnglish_server/docker-compose.yml"
          target: "/home/ze"

#      - name: Send docker-compose.yml to server
#        run: scp ./ZubrilkaEnglish_server/docker-compose.yml /home/ze
#        env:
#          SERVER_IP: ${{ secrets.SERVER_IP }}
#          SERVER_USERNAME: ${{ secrets.SERVER_USERNAME }}
#          SERVER_PASSWORD: ${{ secrets.SERVER_PASSWORD }}


      - name: Display a message
        run: echo "Hello from GitHub Actions"
