version: '3.8'

services:
  certbot:
    container_name: certbot
    build: .
#    command: certonly --standalone --preferred-challenges http --agree-tos  --email kmn.petrichenko@yandex.ru -d 608686.cloud4box.ru
#    certbot renew --dry-run   для перезаписи
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - "/sertbot:/etc/letsencrypt" #ключи сертификаты и прочие конфиги бота. скрипты по пути ......
      - "/sertbot/lib:/var/lib/letsencrypt" # просто пустая наверное
      - "/sertbot/log:/var/log"
