version: '3.8'

services:
  certbot:
    image: certbot/certbot
#      args:
#        DOMAIN_NAME: ${DOMAIN_NAME}
#    environment:
#      DOMAIN_NAME: ${DOMAIN_NAME}
#    command: /bin/sh -c "chmod +x /script.sh && /script.sh"
    command:
      - certbot certonly --email kmn.petrichenko@yandex.ru -d 608686.cloud4box.ru
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - "/sertbot:/etc/letsencrypt" #ключи сертификаты и прочие конфиги бота. скрипты по пути ......
      - "/sertbot/lib:/var/lib/letsencrypt" # просто пустая наверное
